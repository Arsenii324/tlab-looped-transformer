# T-Lab looped-transformer — dependencies for training, eval, and the correctness gates.
#
# There was no root manifest until 2026-08-23: the only ones in the repo were 23 copies of
# ds_*/requirements.txt listing unpinned numpy/datasets/tokenizers for DataSphere jobs. The task
# asks for runnable train and eval scripts, and a grader cloning this repo had nothing to install
# from. Versions below are the ones this project actually ran on, given as floors rather than exact
# pins so a fresh environment resolves — except where an exact version is load-bearing (see torch).

# THE INTERPRETER EVERY LOCAL NUMBER IN THIS REPORT CAME FROM, stated because it was never written
# down and the obvious guess is wrong. All local/MPS runs used the **anaconda base** interpreter:
#     /Users/a2mogus/anaconda3/bin/python3   Python 3.11.0, torch 2.8.0, numpy 2.4.6, MPS available
# NOT the sibling workspace's `barannikov-work/.venv`, which its CLAUDE.md names as the default: that
# venv carries torch 2.13.0 and has no tokenizers / datasets / huggingface_hub / transformers, so it
# cannot run the data pipeline or test_model.py's Qwen3 comparison. If a local number is being
# reproduced and it disagrees, check the interpreter before checking the code.
# Cloud runs are unaffected and pinned separately: Kaggle uses its own image, DataSphere installs
# torch from the cu121 index inside the job (see DATASPHERE_NOTES.md).

# --- core: needed by src/train.py, src/eval.py, src/model.py ---
torch>=2.4
numpy>=1.26

# --- data pipeline: src/train_tokenizer.py, src/data.py ---
# Only needed to BUILD data/{train,val}.bin and configs/tokenizer.json from FineWeb. Evaluating a
# released checkpoint against the shipped tokenizer.json does not need `datasets`.
tokenizers>=0.20
datasets>=3.0

# --- publishing a checkpoint: src/upload_checkpoint.py ---
huggingface_hub>=0.30

# --- correctness gate ONLY: src/test_model.py check [2] ---
# Compares this repo's DecoderLayer against the real transformers Qwen3DecoderLayer
# (max|diff| = 2.38e-07). Nothing in training or eval imports transformers; if you skip this
# dependency, check [2] is the only thing that fails.
transformers>=4.53

# NOT pinned exactly, deliberately: this project hit a CUDA-13-default torch wheel silently
# reporting cuda.is_available()==False on a T4. On a CUDA box install torch from a CUDA-12 index
# rather than pinning a version here:
#     pip install torch --index-url https://download.pytorch.org/whl/cu121
# Apple Silicon (MPS) and CPU need no index override.
